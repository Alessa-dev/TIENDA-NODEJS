<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buscar Usuarios</title>
    <!-- Estilos de Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Buscar Usuarios por ID</h1>
        <form action="/usuarios/buscar" method="GET" class="mb-4">
            <div class="input-group">
                <input type="text" name="id" id="id" class="form-control" placeholder="ID del Usuario" required>
                <button type="submit" class="btn btn-primary">Buscar</button>
            </div>
        </form>

        <!-- Mostrar los resultados de la búsqueda si hay alguno -->
        <% if (usuarios.length > 0) { %>
            <h2 class="mt-4">Resultado de la Búsqueda:</h2>
            <ul class="list-group">
                <% usuarios.forEach(usuario => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <%= usuario.nombre %> - <%= usuario.email %> - <%= usuario.telefono %>
                        <a href="/usuarios/editar/<%= usuario.id %>" class="btn btn-outline-primary">Editar</a>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p class="mt-4">No se encontraron resultados para el ID proporcionado.</p>
        <% } %>
    </div>

    <!-- Scripts de Bootstrap (opcional) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>